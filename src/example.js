import React from 'react';
import ReactDOM from 'react-dom';

import Comments from './Comments';

ReactDOM.render(
  <Comments isAdmin id={214790} type="Topic" />,
  document.querySelector('#app')
);
